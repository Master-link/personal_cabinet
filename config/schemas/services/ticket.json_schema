{
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "properties": {
    "kind": { "enum": ["sms_gate", "tech_support", "custom", "calls_bot"] }
  },
  "required": ["kind"]
}
